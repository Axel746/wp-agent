import { db } from "@wp-agent-studio/database";import { requirePageSession } from "@/lib/auth";
export const dynamic="force-dynamic";
export default async function Deployments(){const session=await requirePageSession();const deployments=await db.deployment.findMany({where:{project:{workspaceId:session.workspaceId}},include:{project:true,wordpressSite:true},orderBy:{createdAt:"desc"}});return <><p className="eyebrow">Traçabilité exacte</p><h1 className="title">Déploiements</h1><p className="lead">Un succès n’est affiché qu’après une opération réelle. Les réponses WordPress et l’empreinte des artefacts sont conservées.</p><section className="card full" style={{marginTop:28}}><table><thead><tr><th>Projet</th><th>Cible</th><th>État</th><th>Date</th><th>Réponses</th></tr></thead><tbody>{deployments.map(item=><tr key={item.id}><td>{item.project.name}</td><td>{item.wordpressSite.name}<br/><small>{item.target}</small></td><td><span className={`badge ${item.status==="FAILED"?"error":""}`}>{item.status}</span></td><td>{item.createdAt.toLocaleString("fr-FR")}</td><td><details><summary>Voir</summary><pre className="code">{JSON.stringify(item.exactResponses,null,2)}</pre></details></td></tr>)}</tbody></table>{!deployments.length&&<p className="muted">Aucun déploiement exécuté.</p>}</section></>}
