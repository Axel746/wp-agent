export default function Settings(){const rows=[['Mode agents',process.env.AGENT_MODE??'mock'],['Déploiement distant',process.env.ENABLE_REMOTE_DEPLOYMENT==='true'?'activé':'désactivé'],['Cibles privées',process.env.ENABLE_PRIVATE_NETWORK_TARGETS==='true'?'autorisées':'bloquées'],['Tours maximum',process.env.MAX_AGENT_TURNS??'12'],['Cycles de correction',process.env.MAX_FIX_CYCLES??'2']];return <><p className="eyebrow">Configuration sûre</p><h1 className="title">Paramètres</h1><p className="lead">Les valeurs sensibles ne sont jamais affichées. Modifiez les variables d’environnement puis redémarrez les services.</p><section className="card full" style={{marginTop:28}}><div className="list">{rows.map(([label,value])=><div className="list-item" key={label}><strong>{label}</strong><span className="badge">{value}</span></div>)}</div></section></>}
