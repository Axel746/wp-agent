import { db } from "@wp-agent-studio/database";import { requirePageSession } from "@/lib/auth";
export const dynamic="force-dynamic";
export default async function Audit(){const session=await requirePageSession();const logs=await db.auditLog.findMany({where:{workspaceId:session.workspaceId},orderBy:{createdAt:"desc"},take:200});return <><p className="eyebrow">Observabilité</p><h1 className="title">Journaux d’audit</h1><p className="lead">Utilisez l’identifiant de corrélation pour comprendre une transition bloquée ou échouée, sans exposer les secrets.</p><section className="card full" style={{marginTop:28}}><table><thead><tr><th>Date</th><th>Action</th><th>Acteur</th><th>Corrélation</th><th>Détails</th></tr></thead><tbody>{logs.map(log=><tr key={log.id}><td>{log.createdAt.toLocaleString("fr-FR")}</td><td>{log.action}</td><td>{log.actorType}</td><td><code>{log.correlationId.slice(0,12)}…</code></td><td><details><summary>Voir</summary><pre className="code">{JSON.stringify(log.metadata,null,2)}</pre></details></td></tr>)}</tbody></table></section></>}
