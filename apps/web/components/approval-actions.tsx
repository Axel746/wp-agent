"use client";import{useState}from"react";import{useRouter}from"next/navigation";import{apiMutation}from"@/lib/client-api";
export function ApprovalActions({id}:{id:string}){const router=useRouter();const[msg,setMsg]=useState("");async function decide(action:"approve"|"reject"){const comment=action==="reject"?prompt("Motif du refus (obligatoire)")??"":"Validé depuis l’interface";if(action==="reject"&&comment.length<3)return;try{await apiMutation(`/api/approvals/${id}/${action}`,"POST",{comment});setMsg("Décision enregistrée");router.refresh()}catch(e){setMsg(e instanceof Error?e.message:"Erreur")}}return <div><div className="actions"><button className="button" onClick={()=>decide("approve")}>Approuver</button><button className="button danger" onClick={()=>decide("reject")}>Refuser</button></div>{msg&&<small className="muted">{msg}</small>}</div>}
