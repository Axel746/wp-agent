"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
export function LoginForm(){const router=useRouter();const[error,setError]=useState("");const[pending,setPending]=useState(false);async function submit(event:React.FormEvent<HTMLFormElement>){event.preventDefault();setPending(true);setError("");const data=Object.fromEntries(new FormData(event.currentTarget));const response=await fetch("/api/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(data)});if(response.ok){router.push("/dashboard");router.refresh()}else{const body=await response.json();setError(body.error?.message??"Connexion impossible")}setPending(false)}return <form onSubmit={submit} className="form-grid" style={{marginTop:24}}>{error&&<p className="alert span-2">{error}</p>}<div className="field span-2"><label>Adresse e-mail</label><input name="email" type="email" defaultValue="admin@wp-agent.local" required/></div><div className="field span-2"><label>Mot de passe</label><input name="password" type="password" defaultValue="change-me" required/></div><button className="button span-2" disabled={pending}>{pending?"Connexion…":"Entrer dans le studio"}</button></form>}
